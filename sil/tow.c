#include <stdio.h>#define N 20#define T 4struct Student { int roll; int term[T]; };int main(void) {    struct Student s[N];    for (int i = 0; i < N; ++i) {        s[i].roll = i;  /* roll numbers now 0–19 */        /* read term totals for terms 0–3 */        if (scanf("%d %d %d %d",                  &s[i].term[0], &s[i].term[1],                  &s[i].term[2], &s[i].term[3]) != 4) {            /* fill with zeros on error */            s[i].term[0] = s[i].term[1] =            s[i].term[2] = s[i].term[3] = 0;        }    }    puts("\n--- Progress Report (0-based) ---");    for (int i = 0; i < N; ++i) {        printf("Roll %2d:", s[i].roll);   /* 0–19 */        for (int t = 0; t < T; ++t)            printf("  T%d=%d", t, s[i].term[t]);  /* T0–T3 */        putchar('\n');    }    return 0;}